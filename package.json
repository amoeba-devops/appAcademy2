{
  "name": "trinity-academy",
  "version": "1.4.6",
  "private": true,
  "description": "Trinity Academy — Clean Architecture (React + NestJS + PostgreSQL)",
  "scripts": {
    "dev": "concurrently -n fe,be -c blue,green \"npm run dev:fe\" \"npm run dev:be\"",
    "dev:fe": "cd frontend-acm && npm run dev",
    "dev:be": "cd backend && npm run start:dev",
    "build:fe": "cd frontend-acm && npm run build",
    "build:be": "cd backend && npm run build",
    "build": "npm run build:fe && npm run build:be",
    "install:all": "cd frontend-acm && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
